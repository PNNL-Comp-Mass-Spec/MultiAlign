<UserControl x:Class="Manassa.Windows.ClusterGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:Manaassa.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:BooleanToDataGridVisibilityModeConverter x:Key="boolConverter"/>
    </UserControl.Resources>
    <Grid>
        <DataGrid
                Name="m_dataGrid" 
                ItemsSource="{Binding}" 
                HorizontalContentAlignment="Center"            
                AlternatingRowBackground="WhiteSmoke"
                AlternationCount="1"
                CanUserAddRows="False"
                AutoGenerateColumns="False"
                Grid.ColumnSpan="6"
                IsTextSearchEnabled="True"
                RowDetailsVisibilityMode="{Binding IsChecked, ElementName=m_detailsChecked, Converter={StaticResource boolConverter}}"
                SelectionChanged="m_dataGrid_SelectionChanged"
            >
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="AliceBlue" />
                            <Setter Property="Foreground" Value="Black" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="LightSteelBlue" />
                            <Setter Property="BorderThickness" Value="1" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns >
                <!--Dataset Information-->
                <DataGridTextColumn         Header="ID"                     Binding="{Binding ID}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="Monoisotopic Mass"      Binding="{Binding MassMonoisotopic}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="NET"                    Binding="{Binding RetentionTime}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="Drift Time"             Binding="{Binding DriftTime}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="Dataset Members"        Binding="{Binding DatasetMemberCount}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="Total Members"          Binding="{Binding MemberCount}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="Tightness"              Binding="{Binding Tightness}" IsReadOnly="True"/>
                <DataGridTextColumn         Header="Ambiguity"              Binding="{Binding AmbiguityScore}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
